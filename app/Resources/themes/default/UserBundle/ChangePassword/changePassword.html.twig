{% extends "UserBundle::layout.html.twig" %}


{% block content %}

    <h1>{% trans %}user.change_password.title{% endtrans %}</h1>

    <div class="view">

    <form action="" {{ form_enctype(form) }} method="post">
        {{ form_widget(form) }}
        <input class="btn" type="submit" value="{{ 'change_password.submit'|trans({}, 'FOSUserBundle') }}" />
    </form>

    </div>

{% endblock %}